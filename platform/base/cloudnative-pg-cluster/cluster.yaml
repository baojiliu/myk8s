apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: postgres
  namespace: postgres
spec:
  instances: 1
  storage:
    size: 10Gi
    storageClass: CHANGE_ME_STORAGECLASS
  bootstrap:
    initdb:
      database: app
      owner: app
      secret:
        name: postgres-app
